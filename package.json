{
  "name": "out-stream",
  "version": "0.1.0",
  "description": "stdout or -o command line file write stream (stream2 for node 0.8.x)",
  "main": "index.js",
  "scripts": {
    "test": "mocha -R list test/*.js"
  },
  "dependencies": {
    "minimist": "0.0.8"
  },
  "devDependencies": {
    "grunt": "^0.4.3",
    "grunt-bump": "0.0.13",
    "mocha": "^1.17.1",
    "should": "^3.1.3"
  }
}
